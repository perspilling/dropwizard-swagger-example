This module contains the representation (DTO) classes that are used to define the API contracts of the
asset-service. The asset-api module may be share with other services/systems that are clients of the 
asset-service.